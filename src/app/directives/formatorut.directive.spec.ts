import { FormatorutDirective } from './formatorut.directive';

describe('FormatorutDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatorutDirective();
    expect(directive).toBeTruthy();
  });
});
